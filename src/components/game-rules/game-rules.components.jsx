import React from 'react';

import './game-rules.styles.css';

const GameRule = () =>{
    return(
        <h2 className="rule">Get points by clicking on an image but don't click on any more than once!</h2>
    )
};

export default GameRule;